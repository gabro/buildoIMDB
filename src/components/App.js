import React from 'react';
import SearchBox from './SearchBox'

export default React.createClass(
   {
      render: function()
      {
         return (
            <SearchBox />
         )
      }
   }
)
